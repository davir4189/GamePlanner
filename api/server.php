<?php
//agregar el objeto bdd
include  './Bdd.php';
class server{
    //separamos el url en partes 
    if(!isset($_COOKIE['token']))
        {
            //si no viene del login lo redirigimos al login
            //si viene del login le damos una cooki token con la funcion
        }
    else{
        //un if con una funcion que mira si el token existe si no error
            //una vez dentro mira la direccion en la que se encustra y el metodo que quiere utilizar
                //dentro de las opciones estan la funciones pertinentes

    }



    //creamos el objeto base de datos

    $bdd=new Bdd()
    
}
?>